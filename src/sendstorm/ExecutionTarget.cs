namespace Sendstorm
{
    /// <summary>
    /// The execution target used in the subscriptions.
    /// </summary>
    public enum ExecutionTarget
    {
        BroadcastThread,
        BackgroundThread,
        UiThread
    }
}
